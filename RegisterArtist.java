

import javax.swing.*;
import java.awt.*;
import static java.awt.Component.LEFT_ALIGNMENT;
import java.awt.event.*;
import java.util.*;

public class RegisterArtist extends RegisterClient implements ActionListener,ItemListener
{
    /*
    ArrayList <DashboardArtist> accounts = new ArrayList<>(); 
    
    public RegisterArtist()
    {
        setVisible(true);
    }
    
    @Override
    public void itemStateChanged(ItemEvent e)
    {
       
    }
    @Override
    public void actionPerformed(ActionEvent e)
    {
       Object source = e.getSource();
       
       if(source == registerButton)
       {
           JOptionPane.showMessageDialog(null,"Account created successfully!");
           accounts.add(new DashboardArtist());
           new SigninArtist();
           setVisible(false);
       }else if(source == goBack)
       {
           new SetupAccountArtist();
           setVisible(false);
       }
       
    
    }
    
    */
    
}
